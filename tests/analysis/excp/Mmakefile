#-----------------------------------------------------------------------------#

THIS_DIR = analysis/excp

#-----------------------------------------------------------------------------#

SUBDIRS=
TESTS=excp_m1-nodepend
TESTS_DIR=../..
include $(TESTS_DIR)/Mmake.common

excp_m1.runtest:
	MC=$(MC) ./excp_runtest.sh

realclean_local:
	$(RM) excp_m1.m
	$(RM) -r Mercury

#-----------------------------------------------------------------------------#
