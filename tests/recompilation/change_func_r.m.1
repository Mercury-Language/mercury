%---------------------------------------------------------------------------%
% vim: ts=4 sw=4 et ft=mercury
%---------------------------------------------------------------------------%

:- module change_func_r.

:- interface.

:- import_module io.

:- pred main(io::di, io::uo) is det.

:- implementation.

:- import_module change_func_r_2.
:- import_module std_util.

main(!IO) :-
    ( if
        semidet_succeed,
        Str = changed_func("OK\n")
    then
        io.write_string(Str, !IO)
    else
        io.write_string("failed\n", !IO)
    ),
    io.write_string(changed_func_2("OK\n"), !IO).
