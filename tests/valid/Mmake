#-----------------------------------------------------------------------------#

main_target: all

#-----------------------------------------------------------------------------#

include ../Mmake

#-----------------------------------------------------------------------------#

SOURCES= \
	complicated_unify.m \
	dcg_test.m \
	det_condition.m \
	det_inference.m \
	det_switch.m \
	easy_nondet_test.m \
	easy_nondet_test_2.m \
	error.m \
	followcode_det_problem.m \
	headvar_not_found.m \
	higher_order.m \
	higher_order2.m\
	higher_order3.m\
	implied_mode.m \
	indexing.m \
	lambda_inference.m\
	lambda_type.m\
	loop.m \
	multidet_prune1.m \
	multidet_test.m \
	name_mangling.m \
	nondet_live.m \
	semidet_disj.m \
	shape_type.m \
	stack_alloc.m \
	switches.m \
	same_length_2.m \
	soln_context.m \
	two_pragma_c_codes.m \
	type_loop.m \
	undead_proc.m \
	uniq_unify.m \
	unreachable_code.m

# The mode system can't handle the following test cases yet:
#	assoc_list.m
#	determinism.m
#	mode_merge_insts.m
# A bug with no_tags functors stops this case from working:
#	unused_args_test2.m
# We don't yet handle det/semidet disjunctions yet, so these
# test cases don't work:
#	loop_in_disj.m
#	same_length_3.m

OBJS =	$(SOURCES:%.m=%.o)
NOS =	$(SOURCES:%.m=%.no)
PROGS =	$(SOURCES:%.m=%)

all:	objs
check:	objs
objs:	$(OBJS)
depend:
clean:
	rm -f *.c *.o *.err2 *.d *.h

#-----------------------------------------------------------------------------#
