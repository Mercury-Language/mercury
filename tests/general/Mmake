#-----------------------------------------------------------------------------#

main_target: check

include ../Mmake

#-----------------------------------------------------------------------------#

PROGS=	set_test arithmetic disj_disj interpreter \
	nasty_nondet nondet_disj nondet_ite petdr1 \
	string__format_test string_test string_test_2 univ \
	environment duplicate_label \
	commit_bug higher_order

# float_test doesn't work because io__write_float
# prints a different number of digits in the C / NU-Prolog implementations.

# string__format_test doesn't work in NU-Prolog, for as yet unknown reasons

DEPENDS=$(PROGS:%=%.depend)
OUTS=	$(PROGS:%=%.out)
EXPS=	$(PROGS:%=%.exp)
RESS=	$(PROGS:%=%.res)
MODS=	$(PROGS:%=%.mod)

depend: $(DEPENDS)

check: $(EXPS) $(OUTS) $(RESS)

mods: $(MODS)

all:	$(PROGS)

#-----------------------------------------------------------------------------#
