#!/bin/sh
#
# INSTALL - installation instructions and installation script.
#
# This file lists the steps needed to install Mercury.
# It also includes shell commands to perform these
# instructions, so that all you need to do to install Mercury
# is to type `sh INSTALL'.

(cd scripts && make) &&
scripts/mmake install
