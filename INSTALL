#!/bin/sh
#
# INSTALL - installation instructions and installation script.
#
# This file lists the steps needed to install Mercury.
# It also includes shell commands to perform these
# instructions, so that all you need to do to install Mercury
# is to type `sh INSTALL'.
#
#-----------------------------------------------------------------------------#
#
# Installation instructions:
# 
# 0.  Obtain the source distribution, e.g. by doing `cvs checkout' in a clean
#     directory, or by ungzipping and untaring the source distribution .tar.gz
#     file.
#
#-----------------------------------------------------------------------------#
#
# 1.  Choose which `make' command to use.
#     If you have GNU Make, it's a good idea to use that.
#     But if you don't, you can use ordinary make.

MAKE=${MAKE:-make}

#-----------------------------------------------------------------------------#
#
# 2.  You need to install the scripts that are used by the makefiles.
#     Type
#     		make install_scripts

$MAKE install_scripts &&
 
#-----------------------------------------------------------------------------#
#
# 3.  Ensure that the *.date timestamp files are present.
#     Type
#
#		make dates

$MAKE dates &&

#     (This step is necessary to ensure that the Makefile doesn't
#     attempt to remake the interface files, which it won't be able
#     to do yet, since we haven't yet installed Mercury!)

#-----------------------------------------------------------------------------#
#
# 4.  Type
# 
# 	make install

$MAKE install &&

#-----------------------------------------------------------------------------#
#
# 5.  (Optional) 
#     Type
# 
# 	make clean

# $MAKE clean &&

#-----------------------------------------------------------------------------#
#
# That's it.

true

#-----------------------------------------------------------------------------#
